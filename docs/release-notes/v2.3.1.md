# Release Notes v2.3.1

## 🐛 Bug Fixes

### Fixed shared mutable default `ConnectionPool`
- `SendgridAPI(api_key=...)` now creates a fresh `ConnectionPool` per instance instead of sharing a single one across all callers
- Previously, multiple `SendgridAPI` instances without an explicit `pool` argument silently shared the same pool
